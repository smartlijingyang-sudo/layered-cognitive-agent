# stop — phase sub-graph (skeleton).
# Single worker: stop_decide -> StopDecision + terminal.
id: stop
version: 0.1.0
region: phase:stop
description: StopPolicy.decide -> StopDecision + terminal. (Skeleton stub.)

graph:
  id: stop
  layer: phase
  purpose: Stop phase — terminal verdict.

nodes:
  - id: decide
    region: phase:stop
    factory: stop__stop_decide
    ins: [state]
    outs: [stop_decision]
