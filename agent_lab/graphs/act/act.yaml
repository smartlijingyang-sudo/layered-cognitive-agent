# act — phase sub-graph (skeleton).
# Workers: decision_to_intent -> intent_allow -> intent_dispatch -> receipt_to_text.
id: act
version: 0.1.0
region: phase:act
description: Build, authorize, dispatch, render a tool call. (Skeleton stub.)

graph:
  id: act
  layer: effect
  purpose: Act phase — single tool call end-to-end.

nodes:
  - id: render
    region: phase:act
    factory: act__receipt_to_text
    ins: [receipt]
    outs: [observation]
