# think — phase sub-graph (skeleton).
# Workers: manifest_to_messages -> prompt_assemble -> llm_call -> response_to_decision -> gate_enforce.
id: think
version: 0.1.0
region: phase:think
description: Brain.think(state) decomposed into 5 single-responsibility workers.
  (Skeleton stub.)

graph:
  id: think
  layer: phase
  purpose: Think phase — manifest -> prompt -> LLM -> Decision.

nodes:
  - id: gate
    region: phase:think
    factory: think__gate_enforce
    ins: [decision]
    outs: [enforced_decision]
