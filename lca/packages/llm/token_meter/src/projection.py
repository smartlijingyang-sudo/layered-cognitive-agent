"""Auto-generated surface skeleton for upstream ``llm/token-meter/src/projection.ts``.

Mirrors the public export surface of the upstream TypeScript source so that
``scripts/check_port_surface.py`` reports parity. Bodies raise
``NotImplementedError`` until a real Python implementation is filled in.

Upstream source: ``llm/token-meter/src/projection.ts``
"""


from __future__ import annotations
from typing import Protocol, TypeAlias

__all__: list[str] = [
    "ContextBreakdownProjection",
    "ContextPressureProjection",
    "TokenUsageProjection",
]

class ContextBreakdownProjection(Protocol):
    """Surface stub for upstream interface ``ContextBreakdownProjection``."""
    pass

class ContextPressureProjection(Protocol):
    """Surface stub for upstream interface ``ContextPressureProjection``."""
    pass

class TokenUsageProjection(Protocol):
    """Surface stub for upstream interface ``TokenUsageProjection``."""
    pass
