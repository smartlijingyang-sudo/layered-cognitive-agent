"""Auto-generated surface skeleton for upstream ``test-support/acp-snapshot/src/index.ts``.

Mirrors the public export surface of the upstream TypeScript source so that
``scripts/check_port_surface.py`` reports parity. Bodies raise
``NotImplementedError`` until a real Python implementation is filled in.

Upstream source: ``test-support/acp-snapshot/src/index.ts``
"""


from __future__ import annotations

from typing import TypeAlias

__all__: list[str] = [
    "AcpTestLaunchOptions",
    "AgentUnderTest",
    "CwdPathMode",
    "HarvestedLog",
    "InputScript",
    "InputStep",
    "LaunchedAcpTestAgent",
    "NormalizeContext",
    "NormalizeOptions",
    "PermissionAnswer",
    "RunOptions",
    "RunResult",
    "Scenario",
    "SnapshotSuiteOptions",
    "defineAcpSnapshotSuite",
    "extractSnapshotSpillPaths",
    "launchAcpTestAgent",
    "normalizeSessionLog",
    "normalizeStdout",
    "refreshFixtureReplacements",
    "runScenario",
    "scrubRequestHeaders",
    "scrubSystemPrompts",
    "scrubToolSchemas",
    "stabilizeFixtureMessageIds",
    "stabilizeRefreshLog",
    "tokenizeSessionFixtureCwd",
]

AcpTestLaunchOptions: TypeAlias = object  # port: surface stub

AgentUnderTest: TypeAlias = object  # port: surface stub

CwdPathMode: TypeAlias = object  # port: surface stub

HarvestedLog: TypeAlias = object  # port: surface stub

InputScript: TypeAlias = object  # port: surface stub

InputStep: TypeAlias = object  # port: surface stub

LaunchedAcpTestAgent: TypeAlias = object  # port: surface stub

NormalizeContext: TypeAlias = object  # port: surface stub

NormalizeOptions: TypeAlias = object  # port: surface stub

PermissionAnswer: TypeAlias = object  # port: surface stub

RunOptions: TypeAlias = object  # port: surface stub

RunResult: TypeAlias = object  # port: surface stub

Scenario: TypeAlias = object  # port: surface stub

SnapshotSuiteOptions: TypeAlias = object  # port: surface stub

defineAcpSnapshotSuite = None  # port: surface stub (reexport)

extractSnapshotSpillPaths = None  # port: surface stub (reexport)

launchAcpTestAgent = None  # port: surface stub (reexport)

normalizeSessionLog = None  # port: surface stub (reexport)

normalizeStdout = None  # port: surface stub (reexport)

refreshFixtureReplacements = None  # port: surface stub (reexport)

runScenario = None  # port: surface stub (reexport)

scrubRequestHeaders = None  # port: surface stub (reexport)

scrubSystemPrompts = None  # port: surface stub (reexport)

scrubToolSchemas = None  # port: surface stub (reexport)

stabilizeFixtureMessageIds = None  # port: surface stub (reexport)

stabilizeRefreshLog = None  # port: surface stub (reexport)

tokenizeSessionFixtureCwd = None  # port: surface stub (reexport)
