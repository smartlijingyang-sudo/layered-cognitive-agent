"""Auto-generated surface skeleton for upstream ``compaction/compaction-basic/src/config.ts``.

Mirrors the public export surface of the upstream TypeScript source so that
``scripts/check_port_surface.py`` reports parity. Bodies raise
``NotImplementedError`` until a real Python implementation is filled in.

Upstream source: ``compaction/compaction-basic/src/config.ts``
"""


from __future__ import annotations

__all__: list[str] = [
    "TargetPressureConfigError",
    "resolveCompactSpec",
    "resolveConfig",
    "resolveTargetPolicy",
]

def resolveCompactSpec(*args: object, **kwargs: object) -> object:
    """Surface stub for upstream function ``resolveCompactSpec``."""
    raise NotImplementedError("port resolveCompactSpec from compaction/compaction-basic/src/config.ts")

def resolveConfig(*args: object, **kwargs: object) -> object:
    """Surface stub for upstream function ``resolveConfig``."""
    raise NotImplementedError("port resolveConfig from compaction/compaction-basic/src/config.ts")

def resolveTargetPolicy(*args: object, **kwargs: object) -> object:
    """Surface stub for upstream function ``resolveTargetPolicy``."""
    raise NotImplementedError("port resolveTargetPolicy from compaction/compaction-basic/src/config.ts")

class TargetPressureConfigError:
    """Surface stub for upstream class ``TargetPressureConfigError``."""

    def __init__(self, *args: object, **kwargs: object) -> None:
        raise NotImplementedError("port TargetPressureConfigError.__init__ from compaction/compaction-basic/src/config.ts")
