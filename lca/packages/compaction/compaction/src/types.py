"""Auto-generated surface skeleton for upstream ``compaction/compaction/src/types.ts``.

Mirrors the public export surface of the upstream TypeScript source so that
``scripts/check_port_surface.py`` reports parity. Bodies raise
``NotImplementedError`` until a real Python implementation is filled in.

Upstream source: ``compaction/compaction/src/types.ts``
"""


from __future__ import annotations

from typing import Protocol, TypeAlias

__all__: list[str] = [
    "CompactionId",
    "CompactionResult",
]

CompactionId: TypeAlias = object  # port: surface stub

class CompactionResult(Protocol):
    """Surface stub for upstream interface ``CompactionResult``."""
    pass
