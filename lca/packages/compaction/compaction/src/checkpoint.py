"""Auto-generated surface skeleton for upstream ``compaction/compaction/src/checkpoint.ts``.

Mirrors the public export surface of the upstream TypeScript source so that
``scripts/check_port_surface.py`` reports parity. Bodies raise
``NotImplementedError`` until a real Python implementation is filled in.

Upstream source: ``compaction/compaction/src/checkpoint.ts``
"""


from __future__ import annotations

from typing import TypeAlias

__all__: list[str] = [
    "CompactionCheckpointSource",
    "compactCheckpointSource",
    "isCompactCheckpointSource",
]

CompactionCheckpointSource: TypeAlias = object  # port: surface stub

def compactCheckpointSource(*args: object, **kwargs: object) -> object:
    """Surface stub for upstream function ``compactCheckpointSource``."""
    raise NotImplementedError("port compactCheckpointSource from compaction/compaction/src/checkpoint.ts")

def isCompactCheckpointSource(*args: object, **kwargs: object) -> object:
    """Surface stub for upstream function ``isCompactCheckpointSource``."""
    raise NotImplementedError("port isCompactCheckpointSource from compaction/compaction/src/checkpoint.ts")
