"""Auto-generated surface skeleton for upstream ``compaction/compaction-tool-result-pruner/src/types.ts``.

Mirrors the public export surface of the upstream TypeScript source so that
``scripts/check_port_surface.py`` reports parity. Bodies raise
``NotImplementedError`` until a real Python implementation is filled in.

Upstream source: ``compaction/compaction-tool-result-pruner/src/types.ts``
"""


from __future__ import annotations

from typing import Protocol

__all__: list[str] = [
    "PruneResult",
    "PrunedEntry",
    "ResolvedConfig",
    "ToolResultPruneConfig",
]

class PruneResult(Protocol):
    """Surface stub for upstream interface ``PruneResult``."""
    pass

class PrunedEntry(Protocol):
    """Surface stub for upstream interface ``PrunedEntry``."""
    pass

class ResolvedConfig(Protocol):
    """Surface stub for upstream interface ``ResolvedConfig``."""
    pass

class ToolResultPruneConfig(Protocol):
    """Surface stub for upstream interface ``ToolResultPruneConfig``."""
    pass
