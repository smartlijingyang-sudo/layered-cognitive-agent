"""Auto-generated surface skeleton for upstream ``compaction/compaction-tool-result-pruner/src/config.ts``.

Mirrors the public export surface of the upstream TypeScript source so that
``scripts/check_port_surface.py`` reports parity. Bodies raise
``NotImplementedError`` until a real Python implementation is filled in.

Upstream source: ``compaction/compaction-tool-result-pruner/src/config.ts``
"""


from __future__ import annotations

__all__: list[str] = [
    "DEFAULTS",
    "PRUNE_MARKER",
    "codePointLength",
    "resolveConfig",
]

DEFAULTS = None  # port: surface stub

PRUNE_MARKER = None  # port: surface stub

def codePointLength(*args: object, **kwargs: object) -> object:
    """Surface stub for upstream function ``codePointLength``."""
    raise NotImplementedError("port codePointLength from compaction/compaction-tool-result-pruner/src/config.ts")

def resolveConfig(*args: object, **kwargs: object) -> object:
    """Surface stub for upstream function ``resolveConfig``."""
    raise NotImplementedError("port resolveConfig from compaction/compaction-tool-result-pruner/src/config.ts")
