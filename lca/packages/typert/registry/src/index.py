"""Auto-generated surface skeleton for upstream ``typert/registry/src/index.ts``.

Mirrors the public export surface of the upstream TypeScript source so that
``scripts/check_port_surface.py`` reports parity. Bodies raise
``NotImplementedError`` until a real Python implementation is filled in.

Upstream source: ``typert/registry/src/index.ts``
"""


from __future__ import annotations
from typing import Protocol, TypeAlias

__all__: list[str] = [
    "TypertRegistry",
    "default",
    "typertEndpoint",
    "typertKey",
    "typertPackageKey",
]

TypertRegistry = None  # port: surface stub (reexport)

default = None  # port: surface stub (reexport)

typertEndpoint = None  # port: surface stub (reexport)

typertKey = None  # port: surface stub (reexport)

typertPackageKey = None  # port: surface stub (reexport)
