"""Auto-generated surface skeleton for upstream ``typert/generator/src/index.ts``.

Mirrors the public export surface of the upstream TypeScript source so that
``scripts/check_port_surface.py`` reports parity. Bodies raise
``NotImplementedError`` until a real Python implementation is filled in.

Upstream source: ``typert/generator/src/index.ts``
"""


from __future__ import annotations
from typing import Protocol, TypeAlias

__all__: list[str] = [
    "AnalysisMode",
    "DiscoveredTypertPackage",
    "FaceModelEmitter",
    "ModelEmitResult",
    "TypeGraphRenderError",
    "TypeGraphRenderer",
    "TypertAnalysisError",
    "TypertEmitError",
    "WorkspaceAnalyzer",
    "WorkspaceAnalyzerOptions",
    "WorkspaceCaches",
    "WorkspaceEmitResult",
    "WorkspaceTypertGenerator",
]

AnalysisMode: TypeAlias = object  # port: surface stub

DiscoveredTypertPackage: TypeAlias = object  # port: surface stub

ModelEmitResult: TypeAlias = object  # port: surface stub

WorkspaceAnalyzerOptions: TypeAlias = object  # port: surface stub

WorkspaceEmitResult: TypeAlias = object  # port: surface stub

FaceModelEmitter = None  # port: surface stub (reexport)

TypeGraphRenderError = None  # port: surface stub (reexport)

TypeGraphRenderer = None  # port: surface stub (reexport)

TypertAnalysisError = None  # port: surface stub (reexport)

TypertEmitError = None  # port: surface stub (reexport)

WorkspaceAnalyzer = None  # port: surface stub (reexport)

WorkspaceCaches = None  # port: surface stub (reexport)

WorkspaceTypertGenerator = None  # port: surface stub (reexport)
