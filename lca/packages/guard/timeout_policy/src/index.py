"""Auto-generated surface skeleton for upstream ``guard/timeout-policy/src/index.ts``.

Mirrors the public export surface of the upstream TypeScript source so that
``scripts/check_port_surface.py`` reports parity. Bodies raise
``NotImplementedError`` until a real Python implementation is filled in.

Upstream source: ``guard/timeout-policy/src/index.ts``
"""


from __future__ import annotations

__all__: list[str] = [
    "TOOL_TIMEOUT",
    "apply",
    "inject",
    "name",
]

TOOL_TIMEOUT = None  # port: surface stub

inject = None  # port: surface stub

name = None  # port: surface stub

def apply(*args: object, **kwargs: object) -> object:
    """Surface stub for upstream function ``apply``."""
    raise NotImplementedError("port apply from guard/timeout-policy/src/index.ts")
