"""Auto-generated surface skeleton for upstream ``sdk/client/src/index.ts``.

Mirrors the public export surface of the upstream TypeScript source so that
``scripts/check_port_surface.py`` reports parity. Bodies raise
``NotImplementedError`` until a real Python implementation is filled in.

Upstream source: ``sdk/client/src/index.ts``
"""


from __future__ import annotations
from typing import Protocol, TypeAlias

__all__: list[str] = [
    "ContentBlock",
    "DeepSeekHarness",
    "DeepSeekHarnessOptions",
    "HarnessClient",
    "HarnessClientOptions",
    "HarnessNotification",
    "HarnessSession",
    "JsonRpcResponseError",
    "NotificationFilter",
    "NotificationSubscription",
    "RequestTimeoutError",
    "RunOptions",
    "RunResult",
    "SdkProtocolError",
    "TransportClosedError",
]

ContentBlock: TypeAlias = object  # port: surface stub

DeepSeekHarnessOptions: TypeAlias = object  # port: surface stub

HarnessClientOptions: TypeAlias = object  # port: surface stub

HarnessNotification: TypeAlias = object  # port: surface stub

NotificationFilter: TypeAlias = object  # port: surface stub

NotificationSubscription: TypeAlias = object  # port: surface stub

RunOptions: TypeAlias = object  # port: surface stub

RunResult: TypeAlias = object  # port: surface stub

DeepSeekHarness = None  # port: surface stub (reexport)

HarnessClient = None  # port: surface stub (reexport)

HarnessSession = None  # port: surface stub (reexport)

JsonRpcResponseError = None  # port: surface stub (reexport)

RequestTimeoutError = None  # port: surface stub (reexport)

SdkProtocolError = None  # port: surface stub (reexport)

TransportClosedError = None  # port: surface stub (reexport)
