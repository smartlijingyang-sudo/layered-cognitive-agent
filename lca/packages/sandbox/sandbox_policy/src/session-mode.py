"""Auto-generated surface skeleton for upstream ``sandbox/sandbox-policy/src/session-mode.ts``.

Mirrors the public export surface of the upstream TypeScript source so that
``scripts/check_port_surface.py`` reports parity. Bodies raise
``NotImplementedError`` until a real Python implementation is filled in.

Upstream source: ``sandbox/sandbox-policy/src/session-mode.ts``
"""


from __future__ import annotations
from typing import Protocol, TypeAlias

__all__: list[str] = [
    "SANDBOX_MODES",
    "effectiveSandboxMode",
    "setSandboxMode",
]

SANDBOX_MODES = None  # port: surface stub

def effectiveSandboxMode(*args: object, **kwargs: object) -> object:
    """Surface stub for upstream function ``effectiveSandboxMode``."""
    raise NotImplementedError("port effectiveSandboxMode from sandbox/sandbox-policy/src/session-mode.ts")

def setSandboxMode(*args: object, **kwargs: object) -> object:
    """Surface stub for upstream function ``setSandboxMode``."""
    raise NotImplementedError("port setSandboxMode from sandbox/sandbox-policy/src/session-mode.ts")
