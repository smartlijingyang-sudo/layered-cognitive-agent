"""Auto-generated surface skeleton for upstream ``sandbox/sandbox/src/roots.ts``.

Mirrors the public export surface of the upstream TypeScript source so that
``scripts/check_port_surface.py`` reports parity. Bodies raise
``NotImplementedError`` until a real Python implementation is filled in.

Upstream source: ``sandbox/sandbox/src/roots.ts``
"""


from __future__ import annotations
from typing import Protocol, TypeAlias

__all__: list[str] = [
    "canonicalPath",
    "writableRoots",
]

def canonicalPath(*args: object, **kwargs: object) -> object:
    """Surface stub for upstream function ``canonicalPath``."""
    raise NotImplementedError("port canonicalPath from sandbox/sandbox/src/roots.ts")

def writableRoots(*args: object, **kwargs: object) -> object:
    """Surface stub for upstream function ``writableRoots``."""
    raise NotImplementedError("port writableRoots from sandbox/sandbox/src/roots.ts")
