"""Auto-generated surface skeleton for upstream ``client/ui-tool/src/client/tool/models/diff-card-model.ts``.

Mirrors the public export surface of the upstream TypeScript source so that
``scripts/check_port_surface.py`` reports parity. Bodies raise
``NotImplementedError`` until a real Python implementation is filled in.

Upstream source: ``client/ui-tool/src/client/tool/models/diff-card-model.ts``
"""


from __future__ import annotations
from typing import Protocol, TypeAlias

__all__: list[str] = [
    "CHAT_DIFF_MAX_LINES",
    "DiffCardModel",
    "diffCardModel",
]

CHAT_DIFF_MAX_LINES = None  # port: surface stub

def diffCardModel(*args: object, **kwargs: object) -> object:
    """Surface stub for upstream function ``diffCardModel``."""
    raise NotImplementedError("port diffCardModel from client/ui-tool/src/client/tool/models/diff-card-model.ts")

class DiffCardModel(Protocol):
    """Surface stub for upstream interface ``DiffCardModel``."""
    pass
