"""Auto-generated surface skeleton for upstream ``client/schema-form/src/index.ts``.

Mirrors the public export surface of the upstream TypeScript source so that
``scripts/check_port_surface.py`` reports parity. Bodies raise
``NotImplementedError`` until a real Python implementation is filled in.

Upstream source: ``client/schema-form/src/index.ts``
"""


from __future__ import annotations
from typing import Protocol, TypeAlias

__all__: list[str] = [
    "SchemaNode",
    "deletePath",
    "getPath",
    "hasPath",
    "nodeAtPath",
    "rehydrateSchema",
    "setPath",
    "validateDraft",
]

SchemaNode: TypeAlias = object  # port: surface stub

deletePath = None  # port: surface stub (reexport)

getPath = None  # port: surface stub (reexport)

hasPath = None  # port: surface stub (reexport)

nodeAtPath = None  # port: surface stub (reexport)

rehydrateSchema = None  # port: surface stub (reexport)

setPath = None  # port: surface stub (reexport)

validateDraft = None  # port: surface stub (reexport)
