"""Auto-generated surface skeleton for upstream ``client/ui-attachment/src/index.ts``.

Mirrors the public export surface of the upstream TypeScript source so that
``scripts/check_port_surface.py`` reports parity. Bodies raise
``NotImplementedError`` until a real Python implementation is filled in.

Upstream source: ``client/ui-attachment/src/index.ts``
"""


from __future__ import annotations
from typing import Protocol, TypeAlias

__all__: list[str] = [
    "AttachmentRail",
    "AttachmentRailItem",
    "AttachmentRailLabels",
    "DropOverlay",
    "DropOverlayLabels",
    "ImageGallery",
    "ImageLightbox",
    "ImageLightboxLabels",
    "ImageLoader",
    "MessageImage",
    "MessageImageLabels",
]

AttachmentRailItem: TypeAlias = object  # port: surface stub

AttachmentRailLabels: TypeAlias = object  # port: surface stub

DropOverlayLabels: TypeAlias = object  # port: surface stub

ImageLightboxLabels: TypeAlias = object  # port: surface stub

ImageLoader: TypeAlias = object  # port: surface stub

MessageImageLabels: TypeAlias = object  # port: surface stub

AttachmentRail = None  # port: surface stub (reexport)

DropOverlay = None  # port: surface stub (reexport)

ImageGallery = None  # port: surface stub (reexport)

ImageLightbox = None  # port: surface stub (reexport)

MessageImage = None  # port: surface stub (reexport)
