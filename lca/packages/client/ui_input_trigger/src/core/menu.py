"""Auto-generated surface skeleton for upstream ``client/ui-input-trigger/src/core/menu.ts``.

Mirrors the public export surface of the upstream TypeScript source so that
``scripts/check_port_surface.py`` reports parity. Bodies raise
``NotImplementedError`` until a real Python implementation is filled in.

Upstream source: ``client/ui-input-trigger/src/core/menu.ts``
"""


from __future__ import annotations
from typing import Protocol, TypeAlias

__all__: list[str] = [
    "MENU_CLOSED",
    "exactMatch",
    "menuReduce",
    "seedGroups",
]

MENU_CLOSED = None  # port: surface stub

exactMatch = None  # port: surface stub

menuReduce = None  # port: surface stub

def seedGroups(*args: object, **kwargs: object) -> object:
    """Surface stub for upstream function ``seedGroups``."""
    raise NotImplementedError("port seedGroups from client/ui-input-trigger/src/core/menu.ts")
