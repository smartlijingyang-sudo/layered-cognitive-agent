"""Auto-generated surface skeleton for upstream ``client/web-react/src/index.ts``.

Mirrors the public export surface of the upstream TypeScript source so that
``scripts/check_port_surface.py`` reports parity. Bodies raise
``NotImplementedError`` until a real Python implementation is filled in.

Upstream source: ``client/web-react/src/index.ts``
"""


from __future__ import annotations

from typing import TypeAlias

__all__: list[str] = [
    "ChainRenderOpts",
    "HostObservable",
    "RenderOpts",
    "SessionProvideInfo",
    "SessionProvider",
    "SessionProviderProps",
    "SlotAssemblyError",
    "SlotOwnershipError",
    "SlotRenderer",
    "SlotRendererHost",
    "SnapshotSelectorHook",
    "StaleAuthorizationError",
    "StoreInstanceLike",
    "UseSession",
    "bindSnapshotSelector",
    "createSlotRenderer",
    "useInvoke",
]

ChainRenderOpts: TypeAlias = object  # port: surface stub

HostObservable: TypeAlias = object  # port: surface stub

RenderOpts: TypeAlias = object  # port: surface stub

SessionProvideInfo: TypeAlias = object  # port: surface stub

SessionProviderProps: TypeAlias = object  # port: surface stub

SlotRenderer: TypeAlias = object  # port: surface stub

SlotRendererHost: TypeAlias = object  # port: surface stub

SnapshotSelectorHook: TypeAlias = object  # port: surface stub

StoreInstanceLike: TypeAlias = object  # port: surface stub

UseSession: TypeAlias = object  # port: surface stub

SessionProvider = None  # port: surface stub (reexport)

SlotAssemblyError = None  # port: surface stub (reexport)

SlotOwnershipError = None  # port: surface stub (reexport)

StaleAuthorizationError = None  # port: surface stub (reexport)

bindSnapshotSelector = None  # port: surface stub (reexport)

createSlotRenderer = None  # port: surface stub (reexport)

useInvoke = None  # port: surface stub (reexport)
