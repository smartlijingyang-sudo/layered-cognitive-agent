"""Auto-generated surface skeleton for upstream ``client/ui-deliverables/src/client/index.ts``.

Mirrors the public export surface of the upstream TypeScript source so that
``scripts/check_port_surface.py`` reports parity. Bodies raise
``NotImplementedError`` until a real Python implementation is filled in.

Upstream source: ``client/ui-deliverables/src/client/index.ts``
"""


from __future__ import annotations
from typing import Protocol, TypeAlias

__all__: list[str] = [
    "ProducedFiles",
    "ProducedFilesProps",
    "apply",
    "inject",
    "producedForClosing",
]

ProducedFilesProps: TypeAlias = object  # port: surface stub

inject = None  # port: surface stub

def apply(*args: object, **kwargs: object) -> object:
    """Surface stub for upstream function ``apply``."""
    raise NotImplementedError("port apply from client/ui-deliverables/src/client/index.ts")

ProducedFiles = None  # port: surface stub (reexport)

producedForClosing = None  # port: surface stub (reexport)
