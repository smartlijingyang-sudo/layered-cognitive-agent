"""Auto-generated surface skeleton for upstream ``session/session-projection-cache/src/spec.ts``.

Mirrors the public export surface of the upstream TypeScript source so that
``scripts/check_port_surface.py`` reports parity. Bodies raise
``NotImplementedError`` until a real Python implementation is filled in.

Upstream source: ``session/session-projection-cache/src/spec.ts``
"""


from __future__ import annotations
from typing import Protocol, TypeAlias

__all__: list[str] = [
    "CheckpointIdentity",
    "CheckpointRecord",
    "checkpointIdentity",
    "checkpointRecord",
    "checkpointRow",
    "projectionCacheDomainSpec",
]

CheckpointIdentity: TypeAlias = object  # port: surface stub

CheckpointRecord: TypeAlias = object  # port: surface stub

checkpointIdentity = None  # port: surface stub

checkpointRecord = None  # port: surface stub

checkpointRow = None  # port: surface stub

projectionCacheDomainSpec = None  # port: surface stub
