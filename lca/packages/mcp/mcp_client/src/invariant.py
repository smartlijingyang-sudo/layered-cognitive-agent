"""Auto-generated surface skeleton for upstream ``mcp/mcp-client/src/invariant.ts``.

Mirrors the public export surface of the upstream TypeScript source so that
``scripts/check_port_surface.py`` reports parity. Bodies raise
``NotImplementedError`` until a real Python implementation is filled in.

Upstream source: ``mcp/mcp-client/src/invariant.ts``
"""


from __future__ import annotations

__all__: list[str] = [
    "apply",
    "inject",
    "name",
]

apply = None  # port: surface stub

inject = None  # port: surface stub

name = None  # port: surface stub
