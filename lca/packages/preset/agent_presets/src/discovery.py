"""Auto-generated surface skeleton for upstream ``preset/agent-presets/src/discovery.ts``.

Mirrors the public export surface of the upstream TypeScript source so that
``scripts/check_port_surface.py`` reports parity. Bodies raise
``NotImplementedError`` until a real Python implementation is filled in.

Upstream source: ``preset/agent-presets/src/discovery.ts``
"""


from __future__ import annotations
from typing import Protocol, TypeAlias

__all__: list[str] = [
    "COMPOSITION_FILE",
    "USER_PRESET_DIR",
    "discoverPresets",
    "scanRoot",
]

COMPOSITION_FILE = None  # port: surface stub

USER_PRESET_DIR = None  # port: surface stub

def discoverPresets(*args: object, **kwargs: object) -> object:
    """Surface stub for upstream function ``discoverPresets``."""
    raise NotImplementedError("port discoverPresets from preset/agent-presets/src/discovery.ts")

def scanRoot(*args: object, **kwargs: object) -> object:
    """Surface stub for upstream function ``scanRoot``."""
    raise NotImplementedError("port scanRoot from preset/agent-presets/src/discovery.ts")
