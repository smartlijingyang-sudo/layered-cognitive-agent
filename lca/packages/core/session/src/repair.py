"""Auto-generated surface skeleton for upstream ``core/session/src/repair.ts``.

Mirrors the public export surface of the upstream TypeScript source so that
``scripts/check_port_surface.py`` reports parity. Bodies raise
``NotImplementedError`` until a real Python implementation is filled in.

Upstream source: ``core/session/src/repair.ts``
"""


from __future__ import annotations
from typing import Protocol, TypeAlias

__all__: list[str] = [
    "TOOL_NOT_STARTED",
    "TOOL_OUTCOME_UNKNOWN",
    "interruptedTurnClosers",
]

TOOL_NOT_STARTED = None  # port: surface stub

TOOL_OUTCOME_UNKNOWN = None  # port: surface stub

def interruptedTurnClosers(*args: object, **kwargs: object) -> object:
    """Surface stub for upstream function ``interruptedTurnClosers``."""
    raise NotImplementedError("port interruptedTurnClosers from core/session/src/repair.ts")
