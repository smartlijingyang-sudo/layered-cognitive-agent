"""Auto-generated surface skeleton for upstream ``schedule/schedule/src/index.ts``.

Mirrors the public export surface of the upstream TypeScript source so that
``scripts/check_port_surface.py`` reports parity. Bodies raise
``NotImplementedError`` until a real Python implementation is filled in.

Upstream source: ``schedule/schedule/src/index.ts``
"""


from __future__ import annotations
from typing import Protocol, TypeAlias

__all__: list[str] = [
    "MIN_EVERY_INTERVAL_SECONDS",
    "SCHEDULE_CHANGE_VERSION",
    "ScheduleId",
    "ScheduleInputError",
    "ScheduleLogError",
    "allocateScheduleId",
    "apply",
    "createAfterScheduleRecord",
    "createAtScheduleRecord",
    "createEveryScheduleRecord",
    "decodeScheduleChange",
    "foldScheduleEvents",
    "inject",
    "name",
    "registerScheduleTools",
    "renderEveryReminderBatchFraming",
    "renderReminderFraming",
    "resolveEveryOccurrence",
    "scheduleView",
]

inject = None  # port: surface stub

name = None  # port: surface stub

def apply(*args: object, **kwargs: object) -> object:
    """Surface stub for upstream function ``apply``."""
    raise NotImplementedError("port apply from schedule/schedule/src/index.ts")

MIN_EVERY_INTERVAL_SECONDS = None  # port: surface stub (reexport)

SCHEDULE_CHANGE_VERSION = None  # port: surface stub (reexport)

ScheduleId = None  # port: surface stub (reexport)

ScheduleInputError = None  # port: surface stub (reexport)

ScheduleLogError = None  # port: surface stub (reexport)

allocateScheduleId = None  # port: surface stub (reexport)

createAfterScheduleRecord = None  # port: surface stub (reexport)

createAtScheduleRecord = None  # port: surface stub (reexport)

createEveryScheduleRecord = None  # port: surface stub (reexport)

decodeScheduleChange = None  # port: surface stub (reexport)

foldScheduleEvents = None  # port: surface stub (reexport)

registerScheduleTools = None  # port: surface stub (reexport)

renderEveryReminderBatchFraming = None  # port: surface stub (reexport)

renderReminderFraming = None  # port: surface stub (reexport)

resolveEveryOccurrence = None  # port: surface stub (reexport)

scheduleView = None  # port: surface stub (reexport)
