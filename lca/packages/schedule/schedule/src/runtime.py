"""Auto-generated surface skeleton for upstream ``schedule/schedule/src/runtime.ts``.

Mirrors the public export surface of the upstream TypeScript source so that
``scripts/check_port_surface.py`` reports parity. Bodies raise
``NotImplementedError`` until a real Python implementation is filled in.

Upstream source: ``schedule/schedule/src/runtime.ts``
"""


from __future__ import annotations
from typing import Protocol, TypeAlias

__all__: list[str] = [
    "MAX_TIMER_DELAY_MS",
    "ScheduleRuntime",
]

MAX_TIMER_DELAY_MS = None  # port: surface stub

class ScheduleRuntime:
    """Surface stub for upstream class ``ScheduleRuntime``."""

    def __init__(self, *args: object, **kwargs: object) -> None:
        raise NotImplementedError("port ScheduleRuntime.__init__ from schedule/schedule/src/runtime.ts")
