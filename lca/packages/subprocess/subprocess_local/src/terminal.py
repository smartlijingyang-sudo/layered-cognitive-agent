"""Auto-generated surface skeleton for upstream ``subprocess/subprocess-local/src/terminal.ts``.

Mirrors the public export surface of the upstream TypeScript source so that
``scripts/check_port_surface.py`` reports parity. Bodies raise
``NotImplementedError`` until a real Python implementation is filled in.

Upstream source: ``subprocess/subprocess-local/src/terminal.ts``
"""


from __future__ import annotations
from typing import Protocol, TypeAlias

__all__: list[str] = [
    "LocalTerminalHandle",
]

class LocalTerminalHandle:
    """Surface stub for upstream class ``LocalTerminalHandle``."""

    def __init__(self, *args: object, **kwargs: object) -> None:
        raise NotImplementedError("port LocalTerminalHandle.__init__ from subprocess/subprocess-local/src/terminal.ts")
