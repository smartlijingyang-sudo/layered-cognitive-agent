# Legacy plugin blacklist — plugins exempted from check_plugin_metadata's
# logic_address critical gate.
#
# Format: one plugin id per line, '#' starts a comment.
# Each entry requires a justification (one-line comment after the id).
#
# Examples:
#   my-factory-only-plugin  # test fixture, never deployed to runtime
#
# Currently empty: all 229 plugins declare logic_address
# (see `uv run python scripts/check_plugin_metadata.py` for the live count).
# Add entries only when a plugin genuinely cannot have logic_address
# (e.g. test fixtures, factory-only modules with no runtime instance).

# COMPAT(2026-09-04 PR-3 of note 2026-09-04-plugin-universe-single-entry.md)
# Unreferenced by shipped bundles; kept as test-only fixture for the
# lca.cognition.brain.null_critic / null_synthesizer protocol impls.
# Delete-when: bundles reference these plugins OR lca/cognition/brain/null_* are
# themselves deleted.
lca-critic-null  # test-only fixture; not deployed in standard profile
lca-synthesizer-null  # test-only fixture; not deployed in standard profile
