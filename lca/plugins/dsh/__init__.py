"""DSH bridge plugins — Tier-3.

The DSH bridge allows the LCA agent loop to invoke a downstream DSH
(DeepSeek Harness) subprocess for sub-tasks. It's a complementary loop
provider; the LCA-builtin `loop_cognitive` and `loop_replay` plugins are
the primary loops.
"""
