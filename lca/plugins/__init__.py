"""First-class plugin modules for the LCA harness runtime.

Each sub-package is a harness plugin with a ``manifest`` (PluginManifest)
and a legacy-compatible ``apply()`` function. These modules replace the
hardcoded ``boot_capabilities()`` path.
"""
