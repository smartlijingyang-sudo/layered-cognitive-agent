# HISTORY / RESIDUAL (M1 Issue #14 — 2026-09-15)
#
# This bundle is NO LONGER an edge source of truth.
# Bounded reflect→think ``admit_recovery`` lives on
# ``bundles/outer/phase_main.yaml`` (typed Predicate + loop obligation).
# Plugins may still produce recovery *hints* (routing.next_hint); they must
# not register a parallel edge table.
#
# delete-when: 2026-10-15 — remove this file once `rg declarative-recovery`
# returns only history docs / dated residuals (no profile bundles list).
#
# Prior content (retired): phase.edge.reflect_to_think.recovery with
# LoopGuard maxIterations=1 via lca.plugins.loop.graph.recovery.plugin.

entries: []
