# Think orchestrator (replaces phase.think.subgraph_host).
# Loaded by profiles/fixtures/think-orchestrator-compile.yaml.
# Sole entry: registers phase.think.orchestrator which drives the
# think-orchestrator-graph.yaml topology.

entries:
  - id: phase.think.orchestrator
    name: phase_think_orchestrator
    $module: lca.plugins.loop.phase.think.orchestrator.plugin
    config:
      plan_ref: bundles/think-orchestrator.yaml
      entry_node: think.shortcut
