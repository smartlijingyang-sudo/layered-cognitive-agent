# Think subgraph step executors (no topology — safe to merge with declarative-phase-graph).

entries:
  - id: phase.think.subgraph.shortcut
    name: phase_think_subgraph_shortcut
    $module: lca.plugins.loop.phase.think.subgraph.shortcut.plugin
    config: {}

  - id: phase.think.subgraph.route
    name: phase_think_subgraph_route
    $module: lca.plugins.loop.phase.think.subgraph.route.plugin
    config: {}

  - id: phase.think.subgraph.reason
    name: phase_think_subgraph_reason
    $module: lca.plugins.loop.phase.think.subgraph.reason.plugin
    config: {}

  - id: phase.think.subgraph.classify
    name: phase_think_subgraph_classify
    $module: lca.plugins.loop.phase.think.subgraph.classify.plugin
    config: {}

  - id: phase.think.subgraph.gate
    name: phase_think_subgraph_gate
    $module: lca.plugins.loop.phase.think.subgraph.gate.plugin
    config: {}
