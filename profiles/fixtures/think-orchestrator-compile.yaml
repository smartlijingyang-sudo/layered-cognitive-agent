# Compile fixture for ``bundles/think-orchestrator.yaml``.
# Loads the 5-step phase graph topology + edges bundle to verify
# the new orchestrator bundle resolves cleanly.

fallback_policy:
  default: test_default

bundles:
  - bundles/think-steps.yaml
  - bundles/think-orchestrator.yaml
  - bundles/think-orchestrator-graph.yaml
