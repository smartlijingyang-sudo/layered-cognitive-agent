[project]
name = "taiyi-cosmokit"
version = "0.1.0"
description = "taiyi-cosmokit — 1:1 Python port of @deepseek-ai/cosmokit utility library"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["cosmokit"]
