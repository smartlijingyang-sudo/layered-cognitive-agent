# PR-8 — composer 迁出 + 装饰器/传输插件化 — DEFERRED

PR-8 的改动面覆盖 composer 整体迁出 + 强制装饰器解硬编码 + 团队传输注册化 +
apply_lead_brain isinstance → protocol。子任务单 PR 拆分(8a/8b/8c/8d)仍属
"大 PR",且与 cognitive/team/* 与 application/spawn.py 强耦合,改动一个 seam
可能影响 ~50 文件。

并行实施时被 TPM rate limit 截断(0 tool 完成度);后续单独串行重做建议:

- 8a composer/ 整体迁出 → lca/application/composer/(纯移动 + import 更新)
- 8b LLM 强制装饰器 → registry 模式(provider 默认启用)
- 8c 团队三家传输 → 注册制
- 8d apply_lead_brain isinstance → capability protocol check

每子 PR 独立验证、独立 revert。

Refs: docs/notes/proposed/seam/2026-09-04-plugin-universe-single-entry.md PR-8。
